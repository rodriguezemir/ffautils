name: FFAUtils
version: "${version}"
main: site.zvolcan.fFAUtils.FFAUtils
api-version: "1.21"
dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: false
    packetevents:
      load: BEFORE
      required: false
permissions:
  ffautils.bypass-combat-block:
    description: Allows executing blocked commands while in combat
    default: op
